<Window x:Class="HackathonBEST.MaskEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:HackathonBEST"
        mc:Ignorable="d"
        Title="MaskEditor" Height="300" Width="400">
    <Grid ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        
        <GroupBox Header="GX Mask" Margin="5,0,5,5">
            
        </GroupBox>
        <GroupBox Grid.Column="1" Header="GY Mask" Margin="5,0,5,5">
            
        </GroupBox>
        
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Button Margin="100, 10, 10,10" Height="40" Padding="10">Save</Button>
            <Button Margin="5,10" Height="40" Padding="10">Load default</Button>
            <Button Margin="5,10" Height="40" Padding="10">Cancel</Button>
        </StackPanel>
    </Grid>
</Window>
